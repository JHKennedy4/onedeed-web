machine:
  node:
    version: 6.9

dependencies:
  override:
    - npm install --only=dev
    - npm install -g ember-cli
    - npm install -g bower
    - bower install

deployment:
  production:
    branch: production
    commands:
      - ember deploy production
  staging:
    branch: master
    commands:
      - ember deploy staging
