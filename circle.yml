machine:
  node:
    version: 6.9

dependencies:
  override:
    - npm install --dev
    - bower install

deployment:
  production:
    branch: production
    commands:
      - ember deploy production
  staging:
    branch: master
    commands:
      - ember deploy staging
